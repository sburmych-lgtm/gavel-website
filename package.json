{
  "name": "igor-gavrileyko-final-claude",
  "version": "1.0.0",
  "description": "IGOR GAVRILEYKO — coded visual prototype",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-facts.mjs && node scripts/check-assets.mjs && astro build",
    "build:fast": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port ${PORT:-4321}",
    "start": "astro preview --host 0.0.0.0 --port ${PORT:-4321}",
    "check": "node scripts/check-facts.mjs && node scripts/check-assets.mjs && astro check",
    "qa": "node scripts/qa.mjs"
  },
  "type": "module",
  "dependencies": {
    "astro": "^7.2.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "playwright": "^1.62.1"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
