{
  "name": "igor-gavrileyko-final-claude",
  "version": "1.0.0",
  "description": "IGOR GAVRILEYKO — coded visual prototype",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-facts.mjs && node scripts/check-assets.mjs && node scripts/check-fonts.mjs && astro build",
    "build:fast": "astro build",
    "preview": "astro preview",
    "start": "sirv dist --host 0.0.0.0 --port ${PORT:-4321} --etag",
    "check": "node scripts/check-facts.mjs && node scripts/check-assets.mjs && node scripts/check-fonts.mjs",
    "qa": "node scripts/qa.mjs",
    "qa:fixes": "node scripts/verify-fixes.mjs"
  },
  "type": "module",
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2",
    "sharp": "^0.35.3",
    "sirv-cli": "^3.0.1"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
