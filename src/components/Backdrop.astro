---
---
<div class="backdrop" data-backdrop aria-hidden="true">
  <div class="backdrop-media" data-backdrop-media>
    <canvas class="backdrop-canvas"></canvas>
    <img
      src="/media/canoe-stroke-poster.webp"
      alt=""
      class="backdrop-poster"
      loading="eager"
      decoding="async"
      width="1024"
      height="576"
    />
  </div>
  <div class="backdrop-overlay"></div>
</div>

<style>
  .backdrop {
    position: fixed;
    inset: 0;
    z-index: var(--z-backdrop, 0);
    pointer-events: none;
    overflow: hidden;
    opacity: 1;
    mask-image: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 1) 15%, rgba(0, 0, 1) 85%, transparent 100%);
    -webkit-mask-image: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 1) 15%, rgba(0, 0, 1) 85%, transparent 100%);
    will-change: transform;
  }

  .backdrop-media {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .backdrop-poster {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    filter: saturate(0.35) brightness(0.7);
    transition: opacity 600ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .backdrop-poster[data-hidden] {
    opacity: 0;
  }

  :global(.backdrop-video) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    filter: saturate(0.35) brightness(0.7);
  }

  .backdrop-canvas {
    position: absolute;
    inset: -20px;
    width: calc(100% + 40px);
    height: calc(100% + 40px);
    filter: saturate(0.35) brightness(0.7);
  }

  .backdrop-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      color-mix(in oklab, var(--abyss) 38%, transparent) 0%,
      color-mix(in oklab, var(--petrol) 38%, transparent) 100%
    );
    mix-blend-mode: multiply;
  }

  @media (prefers-reduced-motion: reduce) {
    .backdrop-canvas,
    :global(.backdrop-video) {
      display: none !important;
    }
  }
</style>
